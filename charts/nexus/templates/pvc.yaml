# État persistant de l'api : expenses.db + logs.db + états de lecture (kodi/abs).
# immigration.db y subsiste, orphelin, depuis le retrait de la fonctionnalité Entrée Express.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexus-state
  labels:
    app.kubernetes.io/part-of: nexus
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
  {{- with .Values.persistence.storageClassName }}
  storageClassName: {{ . }}
  {{- end }}
