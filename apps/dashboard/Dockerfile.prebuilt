# Lightweight image using pre-built dist/apps/dashboard from the host.
# Run `npx nx build dashboard --configuration=production` before using this.
FROM nginx:alpine

COPY dist/apps/dashboard/browser /usr/share/nginx/html
COPY apps/dashboard/nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
